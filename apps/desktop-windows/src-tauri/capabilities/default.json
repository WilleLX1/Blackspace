{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The UI may invoke declared Rust commands only; no shell or remote HTTP capability is granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

